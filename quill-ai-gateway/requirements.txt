# Runtime dependencies for the QUILL AI Gateway.
# Pin exact versions before a real deploy; ranges here are for initial
# development. See docs/planning/openai.md §16 for why each is chosen.

Flask>=3.0
Flask-SQLAlchemy>=3.1
psycopg2-binary>=2.9      # PostgreSQL driver
redis>=5.0                 # rate limiting + config cache (app/limits.py)
requests>=2.32             # OpenAI HTTP calls + alert webhooks
gunicorn>=22.0             # production WSGI server (matches quillin-hub)
python-dotenv>=1.0         # local-dev-only .env loading (see run.py)

# Optional upgrade, not required for correctness: app/limits.py's
# count_tokens() uses a conservative ~4-chars-per-token heuristic that is
# safe (it only ever over-estimates, so it can reject a borderline
# request slightly more often than a real tokenizer would, never let an
# oversized one through) but a real tokenizer is more accurate:
# tiktoken>=0.7
